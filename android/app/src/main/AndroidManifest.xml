<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Read-only Health Connect grants. Steps is requested to complete the
         permission flow the product spec asks for, but has no home in our
         data model yet -- see docs/roadmap.md's "possible next steps". -->
    <uses-permission android:name="android.permission.health.READ_STEPS" />
    <uses-permission android:name="android.permission.health.READ_EXERCISE" />
    <uses-permission android:name="android.permission.health.READ_WEIGHT" />
    <uses-permission android:name="android.permission.health.READ_HEART_RATE" />
    <uses-permission android:name="android.permission.health.READ_SLEEP" />

    <!-- Lets HealthConnectClient.getSdkStatus() detect the Health Connect
         provider app on pre-Android-14 devices, where it ships separately
         rather than as part of the OS. -->
    <queries>
        <package android:name="com.google.android.apps.healthdata" />
    </queries>

    <application
        android:name=".MomentumApplication"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:theme="@style/Theme.Momentum"
        android:usesCleartextTraffic="true">
        <!-- usesCleartextTraffic: the dev backend (10.0.2.2 / LAN) has no TLS.
             Scope this via a network security config, or remove entirely,
             before any real deployment. -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Momentum">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Health Connect shows the user "why does this app want my data"
             via this alias rather than a real activity -- required for the
             permissions rationale entry point, not just launched normally. -->
        <activity-alias
            android:name=".ViewPermissionUsageActivity"
            android:exported="true"
            android:targetActivity=".MainActivity"
            android:permission="android.permission.START_VIEW_PERMISSION_USAGE">
            <intent-filter>
                <action android:name="android.intent.action.VIEW_PERMISSION_USAGE" />
                <category android:name="android.intent.category.HEALTH_PERMISSIONS" />
            </intent-filter>
        </activity-alias>
    </application>
</manifest>
