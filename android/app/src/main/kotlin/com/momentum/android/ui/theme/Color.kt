package com.momentum.android.ui.theme

import androidx.compose.ui.graphics.Color

// Matches the web app's Training-pillar blue (frontend/src/styles/index.css
// --series-1) so the two clients read as the same product.
val MomentumBlue = Color(0xFF2A78D6)
val MomentumBlueDark = Color(0xFF3987E5)
