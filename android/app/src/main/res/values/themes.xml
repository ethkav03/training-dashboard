<resources>
    <!-- Compose (MomentumTheme, see ui/theme/Theme.kt) owns all real theming;
         this just gives the manifest a valid, minimal base theme. -->
    <style name="Theme.Momentum" parent="android:Theme.Material.Light.NoActionBar" />
</resources>
